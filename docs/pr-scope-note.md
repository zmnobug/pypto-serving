# PR Scope Note

This PR only claims device-side embedding and sampling support for non-streaming generation.

Streaming generation remains on the existing host-side path and is out of scope for this PR.
